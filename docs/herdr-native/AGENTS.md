# docs/herdr-native — agent notes

1. Core kit only — no product web UI in this track.
2. Mutating actions default **dry-run**; `--apply` required for writes; wipe/nuke also need `--yes` + level.
3. Do not run herdr/agents wipe apply without explicit human approval.
4. Dual-write README when flash/wipe surface changes.
5. Re-score rule 21 (and related) after kit/soak changes.
