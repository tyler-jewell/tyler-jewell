# docs/ports — agent notes

1. Rule 22 is sacred: no hardcoded local app ports; `ports.toml` is the claim SSoT.
2. When adding a local web app, edit that tree’s `ports.toml` first.
3. Dual-write README when the schema changes.
4. Re-score rule 22 after port-related work.
