# Host: MacBook Pro (slug: `macbook-pro`)

**Secondary** host charter for a primary macOS laptop under the Tyler Jewell umbrella.  
Umbrella (`../../AGENTS.md`) remains **overall / sacred**.

## Portable facts (fill from live discovery)

| Fact | Convention |
|------|------------|
| Slug | `macbook-pro` |
| Hostname | output of `hostname` on the machine |
| OS user | `$USER` |
| OS | macOS (Darwin) |
| Home | `$HOME` |
| Role | Primary laptop; day-0 admin + Nix/home-manager |

Do **not** commit machine-private absolute paths into public docs. Use `$HOME`, `~`, and relative repo paths. Concrete values for *this* machine belong in local untracked notes or live `host.toml` generated by agent-kit — never secrets.

## Agent scope on this host class

1. Honor umbrella sacred rules first.
2. Home admin charter at `$HOME/AGENTS.md` (machine home git — separate from this umbrella repo).
3. Declarative user env: `$HOME/system` (Nix + home-manager). Day-0 root (human once):  
   `sudo "$HOME/system/scripts/privileged-setup.sh"`
4. GitHub-oriented clones: `$HOME/github-repos/` (or your preferred clone root).
5. Public Herdr UI: clone **herdr-web** and run its two-step stand-up (`./scripts/serve.sh`).
6. **Mesh-LLM** is the primary local/mesh LLM resource: OpenAI-compatible `OPENAI_BASE_URL` (default `http://127.0.0.1:9337/v1`). Install/serve via upstream `mesh-llm`; discover models live at `/v1/models` — never freeze a model list.
7. AI-first stack after Nix: `agent-kit/scripts/agent-status.sh` (AXI) and `ai-first-setup.sh --dry-run`
8. All agent-invokable CLIs/skills must follow [AXI](https://axi.md) (umbrella rule 11).
9. Do not put app-specific coding standards in this host file.

## Hierarchy

Umbrella → **this host** → deeper trees under `projects/` (or other nested work with their own AGENTS).
